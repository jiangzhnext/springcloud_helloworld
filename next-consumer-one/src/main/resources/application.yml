server:
  port: 8301

spring:
  application:
    name: helloServiceConsumer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  config: classpath:logback.xml